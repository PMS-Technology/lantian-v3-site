// Simple font configuration for Vite/React Router
// Using standard CSS font loading instead of Next.js font system

export const geist = {
  className: "font-sans",
  variable: "--font-sans",
}

// Optional: If you want to load Google Fonts, add them to your root.html or index.html
// This is just a placeholder to maintain compatibility with existing imports
